# 2026-09-20

## 会话记录压缩沉淀(归档)
- 需求:把 2026-09-04 ~ 09-20 的全部会话记录压缩成单一知识库,便于归档后重开新会话不丢经验。
- 产出:`.workbuddy/ARCHIVE.md`(8 节:速览 / 仓库拓扑 / git 全坑 / 架构现状 /
  功能清单(按 commit)/ 测试体系 / 经验教训库 / 待办 + 新会话 checklist)。
- 同步刷新 `.workbuddy/memory/MEMORY.md`:补项目一句话定位 + 指向 ARCHIVE.md,精简为入口级。
- 现状快照:主文件 4754 行,modules 6 个(state/migrate/seat-grid/dragdrop/random-arrange/messages),
  单元测试 7 文件,冒烟 22 批次;权威提交 `fb4de80`。

## 清理无用文档
- 把 9 个已被 ARCHIVE 覆盖/使命完成的历史 .md 从 `.workbuddy/` 顶层移入 `.workbuddy/archive/`:
  overview.md、overview-batch15/dropdown-once-click/group-focus/smart-arrange/teacher-view-drag-fix.md、
  path-A-status.md、seats-generator-review.md、seats-generator-review-v1.3.0.md。
- 新增 `.workbuddy/archive/README.md` 说明各文件来历与「可整体删除」。
- 同步修正 `ARCHIVE.md` 中指向旧文档的 3 处引用(header / §2.3 / §7)。
- 采用**移动而非删除**(可逆);未推送 git(避免把内部笔记删改提交到公开仓库)。
- 仍可进一步清理的非文档杂物(未动,待确认):`__pycache__/`、嵌套误建的 `.workbuddy/.workbuddy/`、
  2 个 .patch(约 640KB)、`check.js`(209KB)、repro_*/diag_*/shot_* 脚本与一批 .png 截图。

## 清理非文档杂物 + 本地经验同步到仓库(用户要求)
- **杂物清理**:把 `__pycache__/`、嵌套误建 `.workbuddy/.workbuddy/`、2 个 .patch(约 640KB)、
  `check.js`(209KB)、`check-syntax.py`/`extract_*`/`verify_dropdowns.py`、全部 `repro_*`/`diag_*`/`shot_*`
  脚本与 `*.png` 截图、`e2e_downloads/` 移到外部备份 `C:/Users/xingz/WorkBuddy/_cleanup_backup_seats_2026-09-20/`。
  清理后 `.workbuddy/` 只剩:ARCHIVE.md / archive/ / memory/ / run_unit_tests.sh / smoke_test*.py。
- **同步经验到仓库**(用户将换机/云端续开发):
  - `git fetch` 发现远端已推进 7 个提交(fb4de80 → 9db61c2),**全是 `static/ClassMaster.html`**(班级管家:
    宿舍/成绩/学段/学生档案),seats-generator 未变 ⇒ 本 worktree 的座位表代码不落后。
  - nested ref bug 又现:`git fetch` 报了 `fb4de80..9db61c2` 但 `origin/master` ref 未落盘(`[gone]`)
    ⇒ 读 `.git/FETCH_HEAD` 取 40-hex 手写 loose ref + `git merge --ff-only` 修好。
  - 复制 ARCHIVE.md / memory/(9 文件)/ archive/(10)/ run_unit_tests.sh / 全量 smoke_test*.py 到推送仓;
    `git rm .workbuddy/overview.md`(改到 archive/);`.gitignore` 加 `__pycache__/`、`*.pyc`。
  - commit `2ddcc18`(42 文件 +6982/-8)已 push;修 ref 后 master=origin/master=2ddcc18。
- **重要认知**:仓库是公开的,且另有 `ClassMaster.html` 由其他会话/机器持续开发;`.workbuddy/` 已纳入 git。
