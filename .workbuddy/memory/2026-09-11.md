# 2026-09-11 工作日志

## 功能:开启「隐藏图标」时同步隐藏「某行某列」坐标标签
- 需求:点击「隐藏图标」(showStudentIcons=false)时,连同座位上的「X排Y列」坐标标签一起隐藏。
- 改动:`static/modules/seat-grid.js` 的 `renderSeatInner` — `seat-number` 改为仅当
  `showIcons` 为真时生成(与性别/标签图标随同一开关共存亡)。约 6 行。
- 测试:新增 `.workbuddy/smoke_test_batch22_hide_icons_hides_coord.py`(8 项断言,
  含空座位场景);回退验证:强制常显时 2 项断言变红,确认测试有效。
- 单元:`bash .workbuddy/run_unit_tests.sh` 7 文件全过。
- 提交:`recover-huangdiv2` master `fb4de80`(776ee3b..fb4de80),已 push 到 origin。
- 注意:本次 push 后 nested-ref bug 未触发,`git status` 干净、master/origin/master 一致。
